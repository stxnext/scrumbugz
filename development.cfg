[buildout]
extends = buildout.cfg


[manage]
recipe = djangorecipe
project = scrumbugz
projectegg = scrumbugz
settings = settings
test = scrumbugz
eggs = ${buildout:eggs}
wsgi = false
fcgi = false
extra-paths = ${buildout:extra-paths}
